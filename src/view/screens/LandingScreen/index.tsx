import { observer } from 'mobx-react';
import React from 'react';
import { appStore } from 'src/mobx/app';

type Props = {};

const LandingScreen = (props: Props) => {
  return (
    <div>
      <p>Landing</p>
    </div>
  );
};

export default LandingScreen;
